# Runbook lab and portfolio previews

The site will expose the six finished Runbook React dashboards under `/lab/runbook/`. A public index will explain the loose brief, synthetic data, test limits and costs, and link to each interactive build, screenshots and source. Each build runs as static files with no backend or model key.

The current homepage stays in place while Nikita reviews two replacement portfolio concepts under `/portfolio-preview/astra/` and `/portfolio-preview/opus/`. Both should lead with Nikita's engineering work and make LOAF one item among several. Use the existing cartoon portrait and verified public links. Do not expose a CV, phone number, private project data or unverified career claims. Preview pages are marked noindex.

The next Runbook design-guidance comparison will have its own page at `/lab/runbook/guidance/` when tested results are ready. It compares improvement routes against the first-run baseline, with exact prompts and browser evidence; it must not imply a scientific model ranking.

Acceptance: six hosted apps load from their nested URLs and accept basic interaction; public index and screenshots work on desktop and mobile; two portfolio preview candidates render; published files have no credential or unintended remote call; the root homepage is unchanged.
